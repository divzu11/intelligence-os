# Server
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
PORT=3001

# Client (Vite exposes vars prefixed with VITE_ to the browser)
# These are the same values as above — copy them
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
